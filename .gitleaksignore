# gitleaks false-positive allowlist.
#
# execution/clob_client.py line 18 documents the bot's public on-chain EOA
# address (used for tx signing, not a secret) in its module docstring.
# Already committed at HEAD (737b285) before this entry was added; gitleaks'
# generic-api-key rule flags it on entropy alone. Allowlisting this specific
# finding, not weakening the scan for real secrets elsewhere.
execution/clob_client.py:generic-api-key:18

# 2026-09-16 reconciliation — findings reviewed one by one, all false positives:
#  - auth.js / static/auth.js: SHA-256 HASH of the dashboard access code (client-side gate,
#    served publicly by design). A hash, not a secret.
#  - odds/nfl_edge.py: the literal `polymarket_tag="nfl"` trips generic-api-key on the
#    substring `polymarket_tag=`.
#  - clob_client.py (root copy): same public EOA address as execution/clob_client.py.
#  - docs/api/ENDPOINTS.md: curl example with a placeholder Authorization header.
#  - tests/security/test_auth.py: fixture value "test-a…345".
auth.js:generic-api-key:3
static/auth.js:generic-api-key:3
odds/nfl_edge.py:generic-api-key:53
clob_client.py:generic-api-key:18
docs/api/ENDPOINTS.md:curl-auth-header:450
tests/security/test_auth.py:generic-api-key:9
