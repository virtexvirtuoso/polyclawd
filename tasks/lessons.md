# Lessons

- 2026-08-18 — Live-ledger audit: when auditing the live wallet on-chain, use the **SDK deposit wallet** (`clob_client._get_client().wallet`, 0xa495c42d…), NOT env POLYMARKET_WALLET_ADDRESS (that's the relayer-signer EOA — nonce 0, holds nothing, and data-api has no record of it; concluding "phantom trading" from the EOA is the trap this line exists to prevent). True lifetime live PnL ≈ −$74.65 (data-api activity: buys $203.20, sells $47.38, redeems $81.17). Three ledgers disagree: governor state −54.59 (frozen Jul 1, missed manual + resolution closes), live_positions Σpnl −86.96 (missing 2 UNBOOKED WINS: Granby +$13.51 and Athens +$18.81 tennis positions were auto-redeemed on-chain ~Jul 15 but still status='open' — position_sync can't resolve rows where market_id=token_id, its Gamma fallback hexes a token id as a market id → "id is invalid" forever), recompute_equity 0.00 (counts only SELL fills; resolution closes write none; live_fills only exists from Jul 14). onchain_balance in /api/live/portfolio is circular (seeded from state.bankroll). RESOLVED same day via Polygonscan: funds are in **pUSD** (Polymarket USD, 0xc011a7e1…e82dfb) — PM migrated collateral off USDC.e; chain audits MUST check pUSD. Wallet holds $23.87 pUSD = deposits 98.53 − buys 203.20 + sells 47.38 + redeems 81.17 exactly. No sweep; the Jun-27 +74.40 bankroll jump was not a deposit. Ground truth: lifetime live PnL −$74.66 (−75.7%) on $98.53 deposited, zero open positions; governor bankroll 41.39 is fiction.

- 2026-08-18 — entry_price units guard added to `log_shadow_trade` (reject outside [0,1]): dead one-off writers (itunes_rss_edge, calendar_edge) had logged Kalshi CENTS (51.0/64.0), producing impossible −50/−64 "per-share" PnLs that polluted every naive SUM. Rows repaired: id 121 was actually a WIN (+0.49); id 122 had side='PASS' (never a position) → pnl NULLed. Also: `live_equity_snapshots` only writes when `/api/live/portfolio` is hit (recompute_equity is endpoint-driven, not scheduled) — added a 4h curl cron 2026-08-18 so the equity series ticks; open discrepancy to investigate someday: snapshot realized_pnl (from live_fills) = 0.0 vs live_portfolio_state realized −54.59, and unrealized=0.0 across 12 open positions (no-token_id positions mark at entry).

- 2026-08-18 — poly_delta "ADVERSE SELECTION CONFIRMED" verdict was FALSE for MispricedCategoryWhale: MCW logs `entry_price` = YES display price with `side="NO"`, but `poly_delta_tracker` fetches the NO-token mid → stored delta ≈ 1−2·YES ≈ −0.14 with zero market movement. Corrected (delta + 2·entry −1) over the same 25 rows = +0.0306 — but quant-agent review (same day) showed that correction itself is flawed: 3 MCW rows are named-outcome markets where the tracker's silent `clob_token_ids[0]` fallback means stored delta was already frame-clean (correct fix = −s, not s+2e−1), and 13 of 25 MCW rows had `entry_price` overwritten by the dedup UPDATE after delta computation → unidentifiable, must drop. Best defensible MCW figure: **+0.0235 ± 0.0190 (n=12, p=0.245)**; corrected overall −0.0005, CI [−0.017, +0.016] — kill gate not triggered, nothing significant either way. Verdict on everything: insufficient evidence; fix conventions, wipe poly_delta columns, re-run.
- 2026-08-18 — FOURTH poly_delta bug found during the fix's TDD pass: run_once compared ISO-T `timestamp` strings against sqlite `datetime()` output (space separator) — same-day rows could NEVER match, so deltas were written only by an accidental once-nightly sweep in the first ~9min after UTC midnight (real lag 1min–24h; fills resolving before midnight never measured — that was the "unexplained" 47% MCW capture loss). The "+60s" label was fiction from day one.
- 2026-08-18 — Scheduler tasks run in `polyclawd-scheduler.service` (own process, `services/scheduler.py`), NOT `polyclawd-api` — restarting only the API leaves scheduler-side code (poly_delta_tracker, shadow writers, pm_leaderboard_scraper) STALE. Any deploy touching scheduler-imported modules must restart BOTH units. (Caught same day: scheduler was still on pre-fix code 2h after the api restart.)
- 2026-08-18 — RISING WALLETS filters shipped (scripts/pm_leaderboard_scraper.py + tests/test_rank_riser_filters.py, 10 tests): riser alert now requires pnl ≥ $10K (kills thin-category negative-PnL climbers) and suppresses+tags `grinder=1` wallets with >50% of open positions in 5m/15m up-or-down markets (live-validated: 0x251c1a28 scores 1.0). Alert-layer only; seeding/graduation unchanged; data-api failure fails OPEN.
- 2026-08-18 — ALL FIXES DEPLOYED to VPS same day: held-side entry_price convention (log_shadow_trade prefers explicit `entry_price`; MCW poly+kalshi, twc weather converter, legacy JSON fallback all pass it), resolver NO-win = 1−entry, dedup UPDATE no longer rewrites entry_price, tracker window cutoffs computed in Python, token[0] guess removed, NULL-entry rows skipped. Tests: tests/test_entry_price_convention.py (13). DB migration: 47 open NO rows complemented (0.543→0.457), all 60 delta readings wiped, study restarted at n=0/50. Backup: /home/linuxuser/backups/shadow_trades-pre-entryfix-2026-08-18.db. Rollback copies: /home/linuxuser/staging-entryfix/prev/. NOTE: 26 pre-existing test failures in the dirty tree (whale suite, smart_wallet_alert, weather_ensemble, options_implied, scorer_edge, sports_engines, security rate-limit) — verified identical on pre-fix baseline, NOT from this change. Root cause: two writer conventions for `entry_price` (MCW = YES price; sports_edge_common = bought-side executable price) against two readers each assuming a different one (shadow PnL assumes YES-price; delta tracker assumes bought-side). No single convention satisfies both readers until one is normalized. Also: small negative deltas (soccer −0.0078) are expected mechanically when entry = executable ask but the snapshot is a mid — half-spread ≠ adverse selection; and "poly_delta_60" is actually captured 60s–6min post-fill (5-min tick).
